FROM alpine:3.19

# Install runtime dependencies
RUN apk add --no-cache ca-certificates tzdata

# Create non-root user
RUN adduser -D -g '' brutus
USER brutus

WORKDIR /home/brutus

# Copy pre-built binary from GoReleaser
COPY brutus /usr/local/bin/brutus

ENTRYPOINT ["brutus"]
CMD ["--help"]
